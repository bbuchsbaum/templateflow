# Set global client

Set global client

## Usage

``` r
tf_set_client(client)
```

## Arguments

- client:

  A \`TemplateFlowClient\`.

## Value

The client (invisibly).

## Examples

``` r
if (FALSE) { # \dontrun{
client <- TemplateFlowClient()
tf_set_client(client)
} # }
```
