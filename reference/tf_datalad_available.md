# Check if DataLad is available

Tests whether the \`datalad\` command-line tool is installed and
accessible.

## Usage

``` r
tf_datalad_available()
```

## Value

TRUE if datalad is available, FALSE otherwise.
